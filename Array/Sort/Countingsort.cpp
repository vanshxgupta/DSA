//we will have a count of number on it index place and then we can sort
                                                                                //like we have array - 1 1 4 2 1 3
//so we can have another array which will store the freuquecy of that element on its index->  indexing-0 1 2 3 4 
                                                                                            frequency -  3 1 1 1
//so now we have to sort the array , tooh 


for(int index=0;index<n;index+){
while(count(index)==0){
    index++;
}//so now we have reached the first no zero index
cout<<index<<end;
count(index)--;

}